#include "main.h"
/**
* _isdigit- checks for a digit (0 through 9).
* @c: int
* Return: 0
*/
int _isdigit(int c)
{
	/* c es mayor que 0 igual que c es menor que 9 */
	if (c >= '0' && c <= '9')
		return (1);
return (0);
}
